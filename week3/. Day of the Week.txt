class Solution {
    public String dayOfTheWeek(int day, int month, int year) {
        String[] days = {
            "Sunday", "Monday", "Tuesday", "Wednesday",
            "Thursday", "Friday", "Saturday"
        };

        // Days from 1971-01-01 (Friday) to the given year
        int totalDays = 0;

        for (int y = 1971; y < year; y++) {
            totalDays += isLeapYear(y) ? 366 : 365;
        }

        int[] daysInMonth = {
            31, 28, 31, 30, 31, 30,
            31, 31, 30, 31, 30, 31
        };

        // Days in previous months
        for (int m = 1; m < month; m++) {
            totalDays += daysInMonth[m - 1];
        }

        // Add leap day if necessary
        if (month > 2 && isLeapYear(year)) {
            totalDays++;
        }

        // Add days before the given date
        totalDays += day - 1;

        // 1971-01-01 was Friday (index 5)
        return days[(5 + totalDays) % 7];
    }

    private boolean isLeapYear(int year) {
        return (year % 400 == 0) ||
               (year % 4 == 0 && year % 100 != 0);
    }
}

Input
day =
31
month =
8
year =
2019
Output
"Saturday"
Expected
"Saturday"